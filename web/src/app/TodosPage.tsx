// TPL-1 milestone-3/task-1 placeholder — real todos CRUD lands in task-3
// once task-2's BFF endpoints exist (.chief/milestone-3/_plan/_todo.md).
// This exists purely to prove `/` routes to something through AppLayout.
export default function TodosPage() {
  return (
    <main className="mx-auto max-w-2xl px-4 py-8">
      <h1 className="text-2xl font-bold text-[var(--sea-ink)]">Todos</h1>
    </main>
  );
}
