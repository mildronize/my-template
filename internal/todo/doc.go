// Package todo is the example domain module for this template. It holds
// the todo CRUD API that exists to demonstrate the identity/auth seam
// working end to end — the domain itself is deliberately minimal.
//
// On fork: this directory is eventually deleted whole (`rm -rf
// internal/todo`) and replaced with your own domain module — but not as
// step one. Study it first (that's what reading this file is), then copy
// it into your new module and get it wired in alongside the original;
// deleting internal/todo is the second-to-last step, not the first. See
// docs/GETTING-STARTED.md Step 4, which spells out why and in what order.
package todo
