// Package todo is the example domain module for this template. It holds
// the todo CRUD API that exists to demonstrate the identity/auth seam
// working end to end — the domain itself is deliberately minimal.
//
// On fork: delete this directory whole (`rm -rf internal/todo`) and add
// your own domain module in its place. See docs/GETTING-STARTED.md.
package todo
